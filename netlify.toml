[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Disable Netlify's "smart skip" behavior
[build.processing]
  skip_processing = false
