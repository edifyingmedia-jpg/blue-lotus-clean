{
  "name": "blue-lotus-monolith",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "openai": "^4.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
