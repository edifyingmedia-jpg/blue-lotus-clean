{
  "name": "blue-lotus-monolith",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "openai": "^4.0.0",
    "lucide-react": "^0.284.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "install-all": "npm install && cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build"
  }
}
